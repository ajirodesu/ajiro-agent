/**
 * GENERATED PLACEHOLDER — do not edit by hand.
 *
 * Real contents are embedded by `scripts/vendor-intel.mjs` through an
 * esbuild plugin at bundle time (lib file texts never touch git). This
 * file exists only so `tsc`, editors, and unit tests resolve the
 * `./intel-libs.gen` import; it is never bundled (the plugin intercepts
 * the import first) and never ships to devices.
 */
export const INTEL_LIB_FILES: Record<string, string> = {};
